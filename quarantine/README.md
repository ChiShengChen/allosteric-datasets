# Quarantine

Files that came out of the conversion without conforming to `spec/FORMAT.md`.

They are held rather than repaired: every repair here means dropping residues from the node set, which shifts every `anchor` index, and that is a decision for the source repository rather than for a converter.

- `curated/3KGF_1` — resnums not strictly ascending within chain np.str_('B')
- `curated/3KGF_2` — resnums not strictly ascending within chain np.str_('B')
- `curated/3PJG` — resnums not strictly ascending within chain np.str_('A')
- `multimer/4HNB_multi` — resnums not strictly ascending within chain np.str_('B')
- `negatives/1A5Z_A` — resnums not strictly ascending within chain np.str_('A')
